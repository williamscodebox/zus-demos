// import { useCounter } from "../../store";

// type ButtonProps = {
//   value: boolean;
// };

// function Button(value: ButtonProps) {
//   const { increment, decrement } = useCounter();
//   const choice = () => {
//     if (value.value) {
//       increment();
//     } else {
//       decrement();
//     }
//   };

//   return (
//     <button onClick={choice} className="flex p-2 border-2">
//       {value.value ? "Increment" : "Decrement"}
//     </button>
//   );
// }

// export default Button;
